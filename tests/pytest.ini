[pytest]
markers =
    early: fixtures that must be started earlier
    later: fixtures that need to be started later

filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
